import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function Page() {
  return <PlaceholderPage title="Crew dashboard" description="Quick access to assigned jobs, map, checklists, photos, and earnings." items={["Today's jobs", "Open map", "Upload before/after photos", "View payout summary"]} />;
}
