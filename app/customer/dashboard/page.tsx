import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function Page() {
  return <PlaceholderPage title="Customer dashboard" description="View quote requests, estimates, scheduled jobs, and invoices." items={["Submit a new request", "Review estimates", "View payment instructions"]} />;
}
