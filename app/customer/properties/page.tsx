import { PlaceholderPage } from "@/components/layout/placeholder-page";

export default function Page() {
  return <PlaceholderPage title="My properties" description="Customer property records and access notes." items={["Address", "Gate notes", "Pet notes", "Hazards"]} />;
}
